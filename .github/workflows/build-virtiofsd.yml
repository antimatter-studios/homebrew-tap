name: Build virtiofsd

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g. 1.13.3)'
        required: true
        default: '1.13.3'

jobs:
  build:
    strategy:
      matrix:
        include:
          - runner: macos-15
            arch: arm64
            artifact: darwin-arm64
            repo: https://github.com/christhomas/virtiofsd.git
            branch: cth/macos
          - runner: macos-15-intel
            arch: x86_64
            artifact: darwin-x86_64
            repo: https://github.com/christhomas/virtiofsd.git
            branch: cth/macos
          - runner: ubuntu-latest
            arch: amd64
            artifact: linux-amd64
            repo: https://github.com/christhomas/virtiofsd.git
            branch: cth/macos

    runs-on: ${{ matrix.runner }}

    steps:
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Clone virtiofsd
        run: |
          git clone --branch ${{ matrix.branch }} --depth 1 \
            ${{ matrix.repo }}

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get install -y -qq libseccomp-dev libcap-ng-dev

      - name: Build release binary
        working-directory: virtiofsd
        run: cargo build --release

      - name: Package binary
        run: |
          mkdir -p dist
          cp virtiofsd/target/release/virtiofsd dist/
          cd dist
          tar czf virtiofsd-${{ inputs.version }}-${{ matrix.artifact }}.tar.gz virtiofsd

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: virtiofsd-${{ matrix.artifact }}
          path: dist/*.tar.gz

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: virtiofsd-v${{ inputs.version }}
          name: virtiofsd v${{ inputs.version }}
          files: artifacts/*.tar.gz
          body: |
            virtiofsd v${{ inputs.version }} — macOS port (Apple Silicon and Intel)

            ## Install via Homebrew
            ```
            brew install antimatter-studios/tap/virtiofsd
            ```
